# frozen_string_literal: true

source "https://rubygems.org"

# Runtime is stdlib-only (json, open3, tmpdir) — the dispatcher shells out to
# gh/git/agent. Gems below are test-only.
gem "minitest"
gem "minitest-reporters"
gem "rake"
gem "rspock", "~> 2.3"
